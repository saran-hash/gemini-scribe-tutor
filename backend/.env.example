# Optional environment variables for the backend example
# If you have an OpenAI API key and want to use OpenAI embeddings, set it here.
# OPENAI_API_KEY=

# Directory to store the local vector database (auto-created)
# VECTOR_DB_DIR=backend/chroma_db

# Uploads directory for temporary files (auto-created)
# UPLOADS_DIR=backend/uploads
